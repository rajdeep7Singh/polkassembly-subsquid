manifestVersion: subsquid.io/v0.1
name: rococo-polkassembly
version: 2
description: |-
  Rococo's governance proposals

build:

deploy:
  addons:
    postgres:
  secrets:
    - NOTIFICATION_API_KEY
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server", "--subscriptions", "--sql-statement-timeout", "3000" ]