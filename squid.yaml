manifestVersion: subsquid.io/v0.1
name: polkassembly-moonbase
version: 1
description: |-
  Moonbase's governance proposals

build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]