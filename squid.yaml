manifestVersion: subsquid.io/v0.1
name: collectives-polkassembly
version: 2
description: |-
  Collective's governance proposals

build:

deploy:
  addons:
    postgres:
  secrets:
    - NOTIFICATION_API_KEY
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]