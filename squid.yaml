manifestVersion: subsquid.io/v0.1
name: xx-polkassembly
version: 1
description: |-
  XX Networks's governance proposals

build:

deploy:
  addons:
    postgres:
  secrets:
      - NOTIFICATION_API_KEY
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]