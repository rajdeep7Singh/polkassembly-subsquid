manifestVersion: subsquid.io/v0.1
name: equilibrium-polkassembly
version: 1
description: |-
  Equilibrium's governance proposals

build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]
