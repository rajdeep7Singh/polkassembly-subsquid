manifestVersion: subsquid.io/v0.1
name: hashed-polkassembly
version: 1
description: |-
  Hashed's governance proposals

build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]