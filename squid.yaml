manifestVersion: subsquid.io/v0.1
name: cere-polkassembly
version: 2
description: |-
  Cere's governance proposals

build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]