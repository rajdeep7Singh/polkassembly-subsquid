manifestVersion: subsquid.io/v0.1
name: polkadot-identity
version: 1
description: |-
  Polkadot's Identity Module

build:

deploy:
  addons:
    postgres:
        config:
  secrets:
    - NOTIFICATION_API_KEY
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server", "--subscriptions" ]