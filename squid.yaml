manifestVersion: subsquid.io/v0.1
name: tanganika-polkassembly
version: 1
description: |-
  Tanganika's governance proposals

build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ] 
  api:
    cmd: [ "npx", "squid-graphql-server" ]
